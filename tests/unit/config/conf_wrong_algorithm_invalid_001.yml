# Specify input
from_data:
  data_stem: ../data/synthetic_lb_data/data
  phase_ids:
  - 0

# Specify work model
work_model:
  name: AffineCombination
  parameters:
    beta: 0.0
    gamma: 0.0

# Specify algorithm
algorithm:
  name: BruteForce
  phase_id: 0
  parameters:
    n_iterations: 8

# Specify output
output_file_stem: output_file
